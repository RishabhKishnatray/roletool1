---
- name: Configure service1
  template:
    src: /home/ubuntu/roletool1/roletool/templates/scylla.yml.j2
    dest: /etc/scylla/scylla.yml  #/etc/elasticsearch/elasticsearch.yml
    owner: ubuntu
    group: ubuntu
    mode: '0644'
  remote_src: true
  become: true
#  remote_src: true
#    mode: '0644'
#  with_items: "{{ templl }}"

  # notify: Restart Elasticsearch

# - name: Configure scylla cassandra
#   template:
#     src: "{{ item }}"
#     dest: /etc/scylla/cassandra
#     owner: root
#     group: root
#     mode: '0644'
#   with_items: "{{ templs }}"
#   notify: Restart scylla







# - name: Give insecure permissions to an existing file
#   ansible.builtin.file:
#     path: "{{ item }}"        #/etc/elasticsearch/elasticsearch.yml 
#     owner: root
# #     group: root
# #     mode: '0777'
# #   with_items: "{{ paths }}"
