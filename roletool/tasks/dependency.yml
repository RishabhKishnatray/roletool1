--- 
- name: Update apt cache
  apt:
    update_cache: yes

- name: directory
  file:
    path: "{{ item }}"
    state: directory
    mode: "0777"
  with_items: "{{ templ }}"

- name: install java
  apt:
    name: "{{ jdkversion }}"
